apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: devops-playground
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: ./charts/devops-playground
      sourceRef:
        kind: GitRepository
        name: devops-playground-charts
      interval: 1m
  targetNamespace: devops-playground
  install:
    createNamespace: true
  values:
    backend:
      image:
        tag: v1.0.3 # {"$imagepolicy": "flux-system:backend:tag"}
    frontend:
      image:
        tag: v1.0.8 # {"$imagepolicy": "flux-system:frontend:tag"}
