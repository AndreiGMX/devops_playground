name: Create Terraform Infrastructure

on:
  workflow_dispatch:

jobs:
  terraform:
    name: 'Terraform'
    runs-on: ubuntu-latest
    
    defaults:
      run:
        shell: bash
        working-directory: ./terraform
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v4
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: eu-north-1

    # NEW STEP: Install AWS CLI (Required for Helm Provider authentication)
    - name: Install AWS CLI
      run: |
        sudo apt-get update
        sudo apt-get install -y unzip curl
        
        ARCH=$(uname -m)
        if [ "$ARCH" = "aarch64" ]; then
          echo "🍎 Detected ARM64 (Apple Silicon) - Downloading ARM installer..."
          curl "https://awscli.amazonaws.com/awscli-exe-linux-aarch64.zip" -o "awscliv2.zip"
        else
          echo "💻 Detected x86_64 (Intel/AMD) - Downloading Standard installer..."
          curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
        fi
        
        unzip -q awscliv2.zip
        sudo ./aws/install --update
        aws --version

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
    
    - name: Terraform Init
      run: terraform init
    
    - name: Terraform Plan
      run: terraform plan -out=tfplan
    
    - name: Terraform Apply
      # 'idempotent' means we can run this multiple times safely.
      # It will see the Cluster exists and just try to add the Helm chart.
      run: terraform apply -auto-approve tfplan